import 'package:flutter/material.dart';

class CreateHotel extends StatelessWidget {
  const CreateHotel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFFF2F7F5),
      body: Center(
        child: Text("CreateHotel"),
      ),
    );
  }
}